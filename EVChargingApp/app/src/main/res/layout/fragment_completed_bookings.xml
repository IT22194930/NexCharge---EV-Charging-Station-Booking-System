<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nexcharge_background_start"
    tools:context=".ui.stationoperator.fragments.tabs.CompletedBookingsTabFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="50dp"
            android:indeterminateTint="@color/nexcharge_primary"
            android:visibility="gone" />

        <!-- Recycler View for Completed Bookings -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewCompletedBookings"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="16dp"
            tools:listitem="@layout/item_operator_booking" />

        <!-- Empty State Layout -->
        <LinearLayout
            android:id="@+id/layoutEmptyState"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <ImageView
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_marginBottom="20dp"
                android:alpha="0.5"
                android:src="@drawable/ic_done_all"
                android:tint="@color/nexcharge_text_hint"
                android:contentDescription="No completed bookings" />

            <TextView
                android:id="@+id/textViewNoBookings"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="No completed bookings found"
                android:textSize="16sp"
                android:textColor="@color/nexcharge_text_secondary"
                android:gravity="center"
                android:layout_marginBottom="8dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Completed bookings will appear here"
                android:textSize="14sp"
                android:textColor="@color/nexcharge_text_hint"
                android:gravity="center" />

        </LinearLayout>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
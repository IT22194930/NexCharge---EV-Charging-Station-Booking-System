<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EVChargingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/nexcharge_primary</item>
        <item name="colorSecondary">@color/nexcharge_secondary</item>
        <item name="colorSurface">@color/nexcharge_surface</item>
        <item name="android:colorBackground">@color/nexcharge_background_start</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorOnBackground">@color/nexcharge_text_primary</item>
        <item name="colorOnSurface">@color/nexcharge_text_primary</item>
    </style>

    <style name="Theme.EVChargingApp" parent="Base.Theme.EVChargingApp" />

    <!-- Splash Screen Theme -->
    <style name="Theme.EVChargingApp.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@drawable/professional_gradient_background</item>
        <item name="colorPrimary">@color/nexcharge_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSurface">@color/nexcharge_surface</item>
        <item name="android:textColorPrimary">@color/nexcharge_text_primary</item>
        <item name="android:textColorSecondary">@color/nexcharge_text_secondary</item>
    </style>

    <!-- Transparent Dialog Theme for Loading -->
    <style name="TransparentDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.3</item>
    </style>

    <!-- Bottom Navigation Text Appearance -->
    <style name="BottomNavigationTextAppearance" parent="TextAppearance.Material3.LabelSmall">
        <item name="android:textSize">11sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textStyle">normal</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Bottom Navigation Active Indicator for Icon Scaling -->
    <style name="BottomNavActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
        <item name="android:width">0dp</item>
        <item name="android:height">0dp</item>
    </style>

        </resources>
